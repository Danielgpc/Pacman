# Pacman Clone

A simple Pacman clone written in C using the SDL2 library.

## Building

To build the project, run the `build.sh` script:

```bash
./build.sh
```

This will compile the source code and create an executable at `build/main`.

## Running

To run the application, execute the compiled binary:

```bash
./build/main
```
